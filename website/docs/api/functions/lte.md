# Function: lte()

> **lte**(`a`, `b`): `boolean`

Defined in: [utils/operation.ts:195](https://github.com/siposdani87/sui-js/blob/433cda184a013753ef41bb6ba61d0b3944481fa1/src/utils/operation.ts#L195)

Checks if the first value is numerically less than or equal to the second.

## Parameters

### a

`unknown`

The first value (treated as a number).

### b

`unknown`

The second value (treated as a number).

## Returns

`boolean`

`true` if `a <= b`.
