# Function: neq()

> **neq**(`a`, `b`): `boolean`

Defined in: [utils/operation.ts:152](https://github.com/siposdani87/sui-js/blob/27883240db7565f37a562e906b93c8d6552ac058/src/utils/operation.ts#L152)

Checks strict inequality between two values.

## Parameters

### a

`unknown`

The first value.

### b

`unknown`

The second value.

## Returns

`boolean`

`true` if `a !== b`.
