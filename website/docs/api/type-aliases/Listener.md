# Type Alias: Listener()

> **Listener** = (`this`, `ev`) => `any`

Defined in: [utils/types.ts:51](https://github.com/siposdani87/sui-js/blob/27883240db7565f37a562e906b93c8d6552ac058/src/utils/types.ts#L51)

Event listener function type.

Represents a callback attached to a DOM element via `addEventListener`.
The `this` context is bound to the target Element.

## Parameters

### this

`Element`

### ev

`Event`

## Returns

`any`
