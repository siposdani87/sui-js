# Type Alias: Listener()

> **Listener** = (`this`, `ev`) => `any`

Defined in: [utils/types.ts:51](https://github.com/siposdani87/sui-js/blob/433cda184a013753ef41bb6ba61d0b3944481fa1/src/utils/types.ts#L51)

Event listener function type.

Represents a callback attached to a DOM element via `addEventListener`.
The `this` context is bound to the target Element.

## Parameters

### this

`Element`

### ev

`Event`

## Returns

`any`
